(ns compiler.core-test
   (:require [clojure.test :refer :all]
             [compiler.logic.lexicon :refer :all]))

(deftest simple-file-test
   (testing "Testing with an simple file"
     (is (= 0 0))))
